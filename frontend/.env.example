# Financial Companion — Frontend environment variables
# Copy to `.env` to override the default API URL.
# If unset, the app calls "/api/v1" which Vite proxies to http://localhost:8000.
VITE_API_URL=http://localhost:8000/api/v1
